export const MAX_UPLOAD_SIZE_BYTES = 10 * 1024 * 1024;

/**
 * Único tipo documental aprovado para a Fase 1 (specification.md §5).
 * A ingestão não classifica o documento — isso é Fase 3.
 */
export const PHASE_1_DOCUMENT_TYPE = 'IDENTITY_DOCUMENT';
